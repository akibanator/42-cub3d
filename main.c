#include "cub3d.h"
#include "mlx.h"

int main(int argc, char *argv[]) {
  // void *mlx;
  // void *mlx_win;
  //
  // mlx = mlx_init();
  // mlx_win = mlx_new_window(mlx, 320, 240, "Cub3d");
  // (void)mlx_win;
  // mlx_loop(mlx);

  check_args(argc, argv);
  return (0);
}
